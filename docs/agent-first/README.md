# Agent-First Harness Notes

This repository follows an agent-first harness pattern:

- `AGENTS.md` is a concise navigation map.
- Durable knowledge is stored in versioned files under `docs/`.
- Execution plans and debt are tracked in `docs/exec-plans/`.
- CI enforces required doc structure with `scripts/check_agent_docs.sh`.
